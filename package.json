{
  "name": "gofind-paviloche",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run start --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  },
  "workspaces": [
    "frontend"
  ]
}